diff --git a/lms/static/custom_css/course_creators.css b/lms/static/custom_css/course_creators.css
index 52feb64..ab6b445 100644
--- a/lms/static/custom_css/course_creators.css
+++ b/lms/static/custom_css/course_creators.css
@@ -170,7 +170,7 @@
     z-index: 2;
 }
 
-.creator-bio.expanded .bio-preview:after {
+.bio-preview.no-fade:after {
     display: none !important;
 }
 
diff --git a/lms/static/js/course_creators.js b/lms/static/js/course_creators.js
index 0853dcb..3ae937a 100644
--- a/lms/static/js/course_creators.js
+++ b/lms/static/js/course_creators.js
@@ -74,21 +74,41 @@
       });
     }
 
+    function updateCollapsedFade() {
+      $('.creator-bio.collapsed').each(function () {
+        var bio = $(this);
+        var preview = bio.find('.bio-preview');
+        var toggle = bio.find('.bio-toggle'); // read more button
+        var contentHeight = preview[0].scrollHeight;
+        var visibleHeight = preview.outerHeight();
+
+        // If content fits fully AND there's no Read More button -> remove fade
+        if (contentHeight <= visibleHeight + 2 && toggle.length === 0) {
+          preview.addClass('no-fade');
+        } else {
+          preview.removeClass('no-fade');
+        }
+      });
+    }
+
     $('.creators-slider').on('setPosition', function () {
       normalizeCreatorCardHeights();
       adjustBioPreviewHeight();
+      updateCollapsedFade();
     });
 
     $(window).on('resize', function () {
       setTimeout(function () {
         normalizeCreatorCardHeights();
         adjustBioPreviewHeight();
+        updateCollapsedFade();
       }, 150);
     });
 
     setTimeout(function () {
       normalizeCreatorCardHeights();
       adjustBioPreviewHeight();
+      updateCollapsedFade();
     }, 300);
 
     /* ===========================================================
